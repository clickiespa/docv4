# Endpoints de trabajos

> Todos los endpoints de este grupo (`GET /jobs/{job_id}`, `POST /jobs/{job_id}/cancel`, `GET /devices/{identifier}/jobs`) son actualmente **implementaciones de código auxiliar**: devuelven datos de marcador de posición y aún no están integrados con el servicio de trabajos.