# Gateways

See [MGD gateway configuration endpoints](mgd_gateways.md) for the platform-facing `/mgd/gateways` API surface.
See [MGD gateway smoke tests](mgd_gateway_smoke_tests.md) for the route-level validation suite, execution command, and sample outputs.
