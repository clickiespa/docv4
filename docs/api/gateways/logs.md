# Logs & observability endpoints

> All endpoints in this group (`GET /devices/{identifier}/logs`, `GET /devices/{identifier}/disk`, `GET /devices/{identifier}/backup/coverage`) are currently **stub implementations** — they return placeholder data and are not backed by a real telemetry pipeline.
>
> See [`not_implemented.md`](not_implemented.md) for their full contracts.
