# Jobs endpoints

> All endpoints in this group (`GET /jobs/{job_id}`, `POST /jobs/{job_id}/cancel`, `GET /devices/{identifier}/jobs`) are currently **stub implementations** — they return placeholder data and are not integrated with the jobs service yet.
